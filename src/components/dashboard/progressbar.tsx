import React, { useState } from "react";

function ProgressBar({ value, max }) {
  const percentage = (value / max) * 100;

  return <div className="">imma kill myself</div>;
}

export default ProgressBar;
